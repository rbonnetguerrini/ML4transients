

data:
  path: "/sps/lsst/groups/transients/HSC/fouchez/raphael/data/rc2_89570"
  visits: null
  test_size: 0.2
  val_size: 0.1
  cutout_types: ['diff', 'coadd']  # Use both diff and coadd cutouts as 2-channel input

training:
  trainer_type: "repulsive_ensemble"
  epochs: 1500
  learning_rate: 0.00016239636313582185
  batch_size: 128
  num_iter_per_epoch: 400
  epoch_decay_start: 80
  num_workers: 4
  output_dir: "/sps/lsst/groups/transients/HSC/fouchez/raphael/training/multichannel_rep_ensemble_50_89570"
  num_models: 50
  
  # Repulsive ensemble specific parameters
  repulsion_strength: 0.1          # Weight for repulsion loss (tune this - try 0.01 to 0.5)
  repulsion_type: "prediction"     # Type of repulsion: 'prediction', 'feature', or 'both'
  temperature: 1.0                 # Temperature for softening predictions
  weight_decay: 0.0                # L2 regularization (optional)
  
  use_tensorboard: true
  tensorboard_log_dir: "runs"
  experiment_name: "multichannel_rep_ensemble_50_89570"
  log_interval: 100
  
  early_stopping:
    enabled: true
    monitor: "fnr"                      
    mode: "min"                         
    patience: 80
    min_delta: 0.001
    start_epoch: 70    
    
  model_params:
    input_shape: [30, 30, 2]            
    num_classes: 2
    num_conv_blocks: 3                  
    base_filters: 64                   
    base_dropout: 0.25                   
    units: 64                          
    in_channels: 2                       
random_state: 41
