# Standard training configuration with multi-channel inputs (diff + coadd)
data:
  path: "/sps/lsst/groups/transients/HSC/fouchez/raphael/data/rc2_89570_r035"
  visits: null
  test_size: 0.2
  val_size: 0.1
  cutout_types: ['diff', 'coadd']  # Use both diff and coadd cutouts as 2-channel input

training:
  trainer_type: "standard"
  epochs: 1500
  learning_rate: 0.00016239636313582185
  batch_size: 128
  num_iter_per_epoch: 400
  epoch_decay_start: 80
  num_workers: 4
  output_dir: "/sps/lsst/groups/transients/HSC/fouchez/raphael/training/standard_multichannel_89570_r035"
  
  use_tensorboard: true
  tensorboard_log_dir: "runs"
  experiment_name: "standard_multichannel_89570_r035"
  log_interval: 50
  
  early_stopping:
    enabled: true
    monitor: "fnr"                     
    mode: "min"                         
    patience: 80
    min_delta: 0.001
    start_epoch: 70 
    
  model_params:
    input_shape: [30, 30, 2]            
    num_classes: 2
    num_conv_blocks: 3                  
    base_filters: 64                   
    base_dropout: 0.25                   
    units: 64                          
    in_channels: 2    
        # Explicitly set 2 input channels

random_state: 42
