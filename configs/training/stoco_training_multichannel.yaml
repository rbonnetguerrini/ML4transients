

data:
  path: "/sps/lsst/groups/transients/HSC/fouchez/raphael/data/rc2_89570"
  visits: null
  test_size: 0.2
  val_size: 0.1
  cutout_types: ['diff', 'coadd'] 

training:
  trainer_type: "stochastic_coteaching"
  epochs: 1500
  learning_rate: 0.003999412311999152
  batch_size: 256
  num_iter_per_epoch: 400
  epoch_decay_start: 80
  num_workers: 4
  weight_decay: 0
  output_dir: "/sps/lsst/groups/transients/HSC/fouchez/raphael/training/multichannel_stoco_89570"
  
  # Stochastic co-teaching specific parameters
  alpha: 32                             # Beta distribution alpha 
  beta: 4                               # Beta distribution beta 
  num_gradual: 10                       # Gradual warmup period (tp_gradual)
  delay: 0                              # Delay before starting rejection 
  exponent: 1                           # Exponent for schedule
  clip: [0.01, 0.99]                    # Min/max clipping for random thresholds
  seed: 42
  
  use_tensorboard: true
  tensorboard_log_dir: "runs"
  experiment_name: "multichannel_stoco_89570"
  log_interval: 100
  
  early_stopping:
    enabled: true
    monitor: "fnr"                      # Monitor False Negative Rate
    mode: "min"                         # Minimize FNR
    patience: 80
    min_delta: 0.001
    
  model_params:
    input_shape: [30, 30, 2]            
    num_classes: 2
    num_conv_blocks: 3                  
    base_filters: 64                   
    base_dropout: 0.25                   
    units: 128                          
    in_channels: 2    

random_state: 42
